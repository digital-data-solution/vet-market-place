const startLicenseCheckJob = () => {
  // This will eventually check if a Vet's license is expired
  console.log('📋 License Cron initialized (Running in background)...');
};

export default startLicenseCheckJob;